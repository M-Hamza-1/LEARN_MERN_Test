// Template Literals use back-ticks (``) rather than the quotes ("") to define a string:  aka (tilde)
// whatever we right in text editor it will gives the output as it is. for new line no need of \n as well.
console.log(`hello 
world ! 
            you are amazing`);
            /* output:
            hello 
            world ! 
                       you are amazing
            
            */


// print variable value without concatintaion with String. =>

let val = 2;

console.log(`val saqure == ${val * 2} `); //output : val saqure == 4 
